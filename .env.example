# Copy to .env for local compose runs. Nothing here is a credential — the one
# secret this stack needs (the Cloudflare tunnel token) is a Docker secret read
# from ./secrets/cloudflare_tunnel_token, never an environment variable.

# Image tag compose builds and runs. CI publishes by commit SHA; leave unset
# locally to get :dev.
SHOT_LAB_SHA=dev
